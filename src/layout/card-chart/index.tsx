import React, { FC } from 'react';
import { Container } from '@mui/material';

import MainCardChart from '../../component/@layout/card-chart';

const CardChart: FC = () => {
   return (
      <Container>
         <MainCardChart />
      </Container>
   );
};

export default CardChart;
